import React from 'react';
import './ActionSelect.css';

export default class ActionSelect extends React.Component  {
    
  render(){
    return(
      <select>
          <option>Action Select to be implemented</option>
      </select>


        /* <select>
            {this.state.location_types.map((value, index) => {
              return <option key={index}> { value.name } </option>
            })}
        </select>
        <label>Location Type</label> */
    );
  }
}
